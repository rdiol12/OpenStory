<Application x:Class="AugurLauncher.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:AugurLauncher"
             StartupUri="MainWindow.xaml">
    <Application.Resources>

        <local:NewTagBrushConverter x:Key="TagBrush"/>
        <local:HexBrushConverter x:Key="HexBrush"/>
        <BooleanToVisibilityConverter x:Key="BoolVis"/>

        <!-- ==== GenMs palette (dark, Nexon-launcher style) ====
             The semantic names are kept from the old parchment skin so the
             rest of the XAML doesn't change: Parchment = card surface,
             MapleWood = card border, Ink* = text tiers. -->
        <Color x:Key="SkyColor">#141519</Color>
        <Color x:Key="SkyTopColor">#22242a</Color>
        <SolidColorBrush x:Key="Parchment"      Color="#222429"/>
        <SolidColorBrush x:Key="ParchmentHover" Color="#2b2e34"/>
        <SolidColorBrush x:Key="ParchmentTrack" Color="#17181c"/>
        <SolidColorBrush x:Key="MapleWood"      Color="#3a3d45"/>
        <SolidColorBrush x:Key="MapleOrange"    Color="#ff8200"/>
        <SolidColorBrush x:Key="MapleOrangeDk"  Color="#e26f00"/>
        <SolidColorBrush x:Key="Gold"           Color="#f4c542"/>
        <SolidColorBrush x:Key="Green"          Color="#4ecb71"/>
        <SolidColorBrush x:Key="Blue"           Color="#4a9fd8"/>
        <SolidColorBrush x:Key="Red"            Color="#ff5c5c"/>
        <SolidColorBrush x:Key="InkPrimary"     Color="#f2f3f5"/>
        <SolidColorBrush x:Key="InkSecondary"   Color="#b7bcc5"/>
        <SolidColorBrush x:Key="InkMuted"       Color="#7f858f"/>
        <SolidColorBrush x:Key="Grey"           Color="#4a4e57"/>

        <FontFamily x:Key="Brand">Segoe UI, Verdana</FontFamily>

        <!-- ==== PLAY button ==== -->
        <Style x:Key="PlayButton" TargetType="Button">
            <Setter Property="FontFamily" Value="{StaticResource Brand}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="24"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Height" Value="62"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="SnapsToDevicePixels" Value="True"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="bd" CornerRadius="6" Background="{StaticResource MapleOrange}">
                            <Border.Effect>
                                <DropShadowEffect Color="#ff8200" BlurRadius="16" ShadowDepth="0" Opacity="0.45"/>
                            </Border.Effect>
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bd" Property="Background" Value="{StaticResource MapleOrangeDk}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="bd" Property="Background" Value="{StaticResource MapleOrangeDk}"/>
                                <Setter TargetName="bd" Property="RenderTransform">
                                    <Setter.Value><ScaleTransform ScaleX="0.98" ScaleY="0.98"/></Setter.Value>
                                </Setter>
                                <Setter TargetName="bd" Property="RenderTransformOrigin" Value="0.5,0.5"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="bd" Property="Background" Value="{StaticResource Grey}"/>
                                <Setter TargetName="bd" Property="Effect" Value="{x:Null}"/>
                                <Setter Property="Foreground" Value="#9aa0aa"/>
                                <Setter Property="Cursor" Value="Arrow"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ==== Window chrome buttons (min / close) ==== -->
        <Style x:Key="ChromeButton" TargetType="Button">
            <Setter Property="Width" Value="30"/>
            <Setter Property="Height" Value="30"/>
            <Setter Property="Foreground" Value="{StaticResource InkSecondary}"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="bd" CornerRadius="4" Background="Transparent">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bd" Property="Background" Value="#2affffff"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ==== File checkbox (picker rows + select-all) ==== -->
        <Style x:Key="FileCheck" TargetType="CheckBox">
            <Setter Property="Foreground" Value="{StaticResource InkPrimary}"/>
            <Setter Property="FontFamily" Value="{StaticResource Brand}"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="CheckBox">
                        <StackPanel Orientation="Horizontal" Background="Transparent">
                            <Border x:Name="box" Width="18" Height="18" CornerRadius="4" Margin="0,0,8,0"
                                    VerticalAlignment="Center"
                                    Background="{StaticResource Parchment}"
                                    BorderBrush="{StaticResource MapleWood}" BorderThickness="1.5">
                                <Path x:Name="check" Data="M 3,8 L 7,12 L 13,3" Stroke="White" StrokeThickness="2.2"
                                      StrokeStartLineCap="Round" StrokeEndLineCap="Round" Visibility="Collapsed"/>
                            </Border>
                            <ContentPresenter VerticalAlignment="Center"/>
                        </StackPanel>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="box" Property="Background" Value="{StaticResource MapleOrange}"/>
                                <Setter TargetName="box" Property="BorderBrush" Value="{StaticResource MapleOrangeDk}"/>
                                <Setter TargetName="check" Property="Visibility" Value="Visible"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="box" Property="BorderBrush" Value="{StaticResource MapleOrange}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ==== Text link button (Skip & Play) ==== -->
        <Style x:Key="LinkButton" TargetType="Button">
            <Setter Property="Foreground" Value="{StaticResource InkSecondary}"/>
            <Setter Property="FontFamily" Value="{StaticResource Brand}"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="Transparent" Padding="6,3">
                            <TextBlock Text="{TemplateBinding Content}" TextDecorations="Underline"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Foreground" Value="{StaticResource MapleOrange}"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- ==== Volume slider ==== -->
        <Style x:Key="AugurSlider" TargetType="Slider">
            <Setter Property="Height" Value="22"/>
            <Setter Property="Minimum" Value="0"/>
            <Setter Property="Maximum" Value="100"/>
            <Setter Property="IsMoveToPointEnabled" Value="True"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Slider">
                        <Grid VerticalAlignment="Center">
                            <Border Height="8" CornerRadius="4" Background="{StaticResource ParchmentTrack}"
                                    BorderBrush="{StaticResource MapleWood}" BorderThickness="1"/>
                            <Track x:Name="PART_Track">
                                <Track.DecreaseRepeatButton>
                                    <RepeatButton Command="Slider.DecreaseLarge">
                                        <RepeatButton.Template>
                                            <ControlTemplate TargetType="RepeatButton">
                                                <Border Height="8" CornerRadius="4" Background="{StaticResource MapleOrange}"/>
                                            </ControlTemplate>
                                        </RepeatButton.Template>
                                    </RepeatButton>
                                </Track.DecreaseRepeatButton>
                                <Track.IncreaseRepeatButton>
                                    <RepeatButton Command="Slider.IncreaseLarge">
                                        <RepeatButton.Template>
                                            <ControlTemplate TargetType="RepeatButton"><Border Background="Transparent"/></ControlTemplate>
                                        </RepeatButton.Template>
                                    </RepeatButton>
                                </Track.IncreaseRepeatButton>
                                <Track.Thumb>
                                    <Thumb Cursor="Hand">
                                        <Thumb.Template>
                                            <ControlTemplate TargetType="Thumb">
                                                <Ellipse Width="18" Height="18" Fill="{StaticResource MapleOrange}" Stroke="White" StrokeThickness="2">
                                                    <Ellipse.Effect><DropShadowEffect Color="#ff8200" BlurRadius="6" ShadowDepth="0" Opacity="0.5"/></Ellipse.Effect>
                                                </Ellipse>
                                            </ControlTemplate>
                                        </Thumb.Template>
                                    </Thumb>
                                </Track.Thumb>
                            </Track>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ==== Resolution pill (RadioButton) ==== -->
        <Style x:Key="Pill" TargetType="RadioButton">
            <Setter Property="FontFamily" Value="{StaticResource Brand}"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Foreground" Value="{StaticResource InkSecondary}"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Margin" Value="0,0,6,6"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <Border x:Name="bd" CornerRadius="4" Padding="12,5"
                                Background="{StaticResource Parchment}" BorderBrush="{StaticResource MapleWood}" BorderThickness="1.5">
                            <ContentPresenter HorizontalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="bd" Property="Background" Value="{StaticResource MapleOrange}"/>
                                <Setter TargetName="bd" Property="BorderBrush" Value="{StaticResource MapleOrangeDk}"/>
                                <Setter Property="Foreground" Value="White"/>
                                <Setter Property="FontWeight" Value="SemiBold"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bd" Property="BorderBrush" Value="{StaticResource MapleOrange}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ==== Text box (server address / port) ==== -->
        <Style x:Key="AugurTextBox" TargetType="TextBox">
            <Setter Property="FontFamily" Value="{StaticResource Brand}"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Foreground" Value="{StaticResource InkPrimary}"/>
            <Setter Property="CaretBrush" Value="{StaticResource MapleOrange}"/>
            <Setter Property="Height" Value="34"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="bd" CornerRadius="4" Padding="10,0"
                                Background="{StaticResource ParchmentHover}"
                                BorderBrush="{StaticResource MapleWood}" BorderThickness="1.5">
                            <ScrollViewer x:Name="PART_ContentHost" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="bd" Property="BorderBrush" Value="{StaticResource MapleOrange}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ==== Dialog buttons (Save / Cancel) ==== -->
        <Style x:Key="SmallPrimary" TargetType="Button">
            <Setter Property="FontFamily" Value="{StaticResource Brand}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Height" Value="42"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="bd" CornerRadius="5" Background="{StaticResource MapleOrange}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True"><Setter TargetName="bd" Property="Background" Value="{StaticResource MapleOrangeDk}"/></Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="SmallSecondary" TargetType="Button" BasedOn="{StaticResource SmallPrimary}">
            <Setter Property="Foreground" Value="{StaticResource InkPrimary}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="bd" CornerRadius="5" Background="{StaticResource Parchment}"
                                BorderBrush="{StaticResource MapleWood}" BorderThickness="1.5">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True"><Setter TargetName="bd" Property="Background" Value="{StaticResource ParchmentHover}"/></Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ==== Progress bar (maple orange on parchment) ==== -->
        <Style x:Key="AugurProgress" TargetType="ProgressBar">
            <Setter Property="Height" Value="12"/>
            <Setter Property="Foreground" Value="{StaticResource MapleOrange}"/>
            <Setter Property="Background" Value="{StaticResource ParchmentTrack}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ProgressBar">
                        <Border CornerRadius="3" Background="{TemplateBinding Background}"
                                BorderBrush="{StaticResource MapleWood}" BorderThickness="1">
                            <Grid ClipToBounds="True">
                                <Border x:Name="PART_Track"/>
                                <Border x:Name="PART_Indicator" HorizontalAlignment="Left"
                                        CornerRadius="3" Background="{TemplateBinding Foreground}">
                                    <Border.Effect>
                                        <DropShadowEffect Color="#ff8200" BlurRadius="8" ShadowDepth="0" Opacity="0.5"/>
                                    </Border.Effect>
                                </Border>
                            </Grid>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

    </Application.Resources>
</Application>
